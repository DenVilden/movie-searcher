overwrite: true
schema: http://localhost:5000
documents: src/apollo/**/*.graphql
generates:
  src/apollo/__generated__/index.ts:
    plugins:
      - typescript
      - typescript-operations
      - typescript-react-apollo
