overwrite: true
schema: 'http://localhost:5000/graphql'
generates:
  server/generated/types.ts:
    plugins:
      - 'typescript'
      - 'typescript-operations'
      - 'typescript-resolvers'
    config:
      useIndexSignature: true
    hooks:
      afterOneFileWrite: 'eslint --fix'
