overwrite: true
schema: src/schema.graphql
generates:
  src/generated/types.ts:
    plugins:
      - typescript
      - typescript-resolvers
    config:
      useIndexSignature: true
      noSchemaStitching: true
      contextType: ../datasources/Movies#Context
hooks:
  afterAllFileWrite:
    - prettier --write
